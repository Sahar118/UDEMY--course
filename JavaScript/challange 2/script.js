/* ------------------- Data 1 ---------------------------*/

let marksBmi1 = 78 / 1.69 ** 2;
let johnsBmi1 = 92 / 1.95 ** 2;
// console.log(marksBmi1);
// console.log(johnsBmi1);

if (marksBmi1 > johnsBmi1) {
  console.log("Mark's BMI is heigher than John's!");
} else {
  console.log("John's BMI is heigher than Mark's!");
}

if (marksBmi1 > johnsBmi1) {
  console.log(
    `Mark's BMI (${marksBmi1}) heigher than John's BMI (${johnsBmi1})!`
  );
} else {
  console.log(
    ` John's BMI (${johnsBmi1}) heigher than Mark's BMI (${marksBmi1})!`
  );
}

// console.log(marksBmi1 > johnsBmi1);

/* ------------------- Data 2 ---------------------------*/
let marksBmi2 = 95 / 1.88 ** 2;
let johnsBmi2 = 85 / 1.76 ** 2;
// console.log(marksBmi2);
// console.log(johnsBmi2);

// console.log(marksBmi2 > johnsBmi2);
if (marksBmi2 > johnsBmi2) {
  console.log("Mark's BMI is heigher than John's!");
} else {
  console.log("John's BMI is heigher than Mark's!");
}

if (marksBmi2 > johnsBmi2) {
  console.log(
    `Mark's BMI (${marksBmi2}) heigher than John's BMI (${johnsBmi2})!`
  );
} else {
  console.log(
    ` John's BMI (${johnsBmi2}) heigher than Mark's BMI (${marksBmi2})!`
  );
}

prompt("Whst's yous favoutie Number?")
